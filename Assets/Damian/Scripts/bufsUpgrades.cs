using UnityEngine;

public class bufsUpgrades : MonoBehaviour
{
    // so dont wanne do this yet
    //savinf for later
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
